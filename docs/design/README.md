# حزمة توثيق UX/Design — نظام المناقصات

<div dir="rtl">

توثّق هذه الحزمة قواعد العمل، الأدوار والصلاحيات، رحلات المستخدمين، وجرد الشاشات لنظام إدارة المناقصات (Smart Tender Management System)، ليكون مرجعًا موحّدًا للفريقين التقني والتصميمي.

> **آخر مزامنة مع الكود: نهاية M4 (Workflow State Machine) — 2026-07-22.** حُدِّثت الحزمة لتعكس تنفيذ مرحلتَي M3 (المراجعة والـChecklist) وM4 (سير العمل الكامل): انتقلت القواعد BR-001/002/004/005/011 والإجراءات ACT-04...ACT-11 من "مخطّط 🔷" إلى "منفّذ ✅"، وأُضيف توثيق الـState Machine المركزية (`services/tenderWorkflow.ts`) وشاشة "مهامي" (SCR-07) وشريط الإجراءات وتبويب المراجعة في SCR-04. الإجراء الوحيد الباقي في نطاق دورة الحياة هو ACT-13 (المرفقات) المؤجَّل إلى M5.

## اصطلاح الترميز

- ✅ **مكتمل / منفّذ**: المستند مكتوب بالكامل، أو القاعدة/الميزة مطبَّقة فعليًا في الكود.
- 🔷 **مخطّط**: المستند لم يُكتب بعد، أو القاعدة/الميزة موثّقة كمتطلب دون تنفيذ حالي.

## مستندات الحزمة

| # | المستند | الوصف | الحالة |
|---|---|---|---|
| 01 | [كتالوج قواعد العمل](./01-business-rules-catalogue.md) | قواعد العمل (BR-xxx)، مخطط انتقال الحالات، جدول الانتقالات، جدول القرارات | ✅ مكتمل |
| 02 | [مصفوفة الأدوار والصلاحيات](./02-roles-permissions-matrix.md) | مصفوفة الأدوار والصلاحيات لكل إجراء (ACT-xxx) | ✅ مكتمل |
| 03 | [رحلات المستخدمين](./03-user-journeys.md) | رحلات المستخدمين الرئيسية (JRN-xxx) عبر دورة حياة المناقصة | ✅ مكتمل |
| 04 | [جرد الشاشات ومواصفاتها](./04-screen-inventory-and-specs.md) | جرد الشاشات (SCR-01…SCR-07) ومواصفاتها | ✅ مكتمل |

## المعرّفات المشتركة

المعرّفات التالية مشتركة بين مستندات الحزمة الأربعة، ويجب استخدامها بشكل متّسق:

| البادئة | المعنى |
|---|---|
| `BR-xxx` | قاعدة عمل (Business Rule) — معرَّفة في المستند 01 |
| `ACT-xxx` | إجراء/فعل يقوم به مستخدم في النظام — يُستخدم في مصفوفة الأدوار (02) وجدول الانتقالات (01) |
| `SCR-xxx` | شاشة في الواجهة — تُعرَّف في جرد الشاشات (04) |
| `JRN-xxx` | رحلة مستخدم كاملة — تُعرَّف في مستند رحلات المستخدمين (03) |

## المصدر المرجعي

هذه الحزمة مبنية وفق المواصفة التصميمية:
`docs/superpowers/specs/2026-07-21-tender-design-docs-design.md`

كما تعتمد قواعد العمل ومعرّفات الحالات على المصدر الأصلي:
`CLAUDE_CODE_PROMPT_Tender_System.md`

</div>
