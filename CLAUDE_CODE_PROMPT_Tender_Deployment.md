# 🚀 برومبت طور النشر: نظام إدارة المناقصات (Deployment Phase Plan)

> **الحالة: لم يبدأ بعد.** هذا الملف يُحدَّث أثناء التنفيذ تمامًا كخطتَي M وH.

---

## ✅ نقطة البداية (2026-07-31)

| | |
| --- | --- |
| **ما قبل هذا الطور** | M0–M8 مكتملة (`v1.0.0`) · H0–H8 مكتملة ومُتحقَّقة (`v2.0.0` → `v2.1.0`) |
| **الاختبارات** | 211 (209 API + 2 واجهة) · تغطية 93% بعتبات حاجبة · 18 فحص دخان داخل الحاوية |
| **البنية الجاهزة** | صورة API مُتحقَّقة بالتشغيل · CI بثلاث وظائف خضراء · إدارة أسرار موثّقة |
| **الفجوة** | **المشروع لم يُنشر قط.** لا سجلّ صور، ولا واجهة محوّاة، ولا خادم، ولا نسخ احتياطي، ولا runbook |

**الحكم بلا مجاملة:** ما أُنجز حتى الآن *بناء وتحقّق*، لا *تشغيل*. الصورة تُبنى في CI ثم تُمحى مع الـrunner، فلا توجد نسخة قابلة للنشر من `v2.1.0`. ونظام بلا إجراء استعادة قاعدة بيانات **مُختبَر** ليس جاهزًا للإنتاج مهما بلغت درجة تقويته.

---

## 🧭 اكتشاف معماري يحكم الطور كلّه (تُحقّق منه بالكود)

`apps/web/src/api/client.ts:15` يعرّف:

```ts
export const API_BASE = '/api/v1';
```

مسار **نسبي**. في التطوير يحلّه بروكسي Vite (`apps/web/vite.config.ts` — يحذف البادئة `/api` ويوجّه إلى `localhost:4000`). في الإنتاج **لا بروكسي**.

ويضاعف الأمرَ أن الكوكي على `sameSite=strict` (H1.2) و`credentials: 'include'`. فلو نُشرت الواجهة على أصل والـAPI على آخر:

- الكوكي لا يُرسَل أصلًا مع الطلبات ⇒ **المصادقة تنهار بصمت**، لا برسالة خطأ واضحة.

**النتيجة:** بروكسي عكسي بأصل واحد **شرط معماري لا تفضيل تشغيلي**. كل قرار في D1 وD2 مبني عليه. البديل (أصلان + `sameSite=none`) يتراجع عن حماية CSRF التي أُنجزت في H1.2 — مرفوض.

---

## ⚙️ قواعد العمل الإلزامية (نفس انضباط المشروع)

1. **اشتغل Milestone بالترتيب.** ممنوع تبدأ D(n) قبل ما تخلّص D(n-1) 100% — إلا لو طلبتُ خلاف ذلك.
2. **كل بند له خطوة تحقق (✅ Verify).** ممنوع تعلّم `[x]` إلا بعد: كتابة الإعداد + **تشغيله فعليًا** + مطابقة معيار القبول. قاعدة H7.1 سارية هنا بأثر مضاعف: هذا طور تشغيل، و«الملف مكتوب» لا يساوي شيئًا.
3. **حدّث هذا الملف** بعد كل بند: `[ ]` → `[x]` واكتب سطر `Verified: <إزاي اتأكدت>`.
4. **Commit بعد كل Milestone** برسالة واضحة: `feat(D1): single-origin web container complete + verified`.
5. **لو بند فشل التحقق:** متكملش. صلّح وسجّل في «سجل مشاكل النشر».
6. **لا تكسر البوابة القائمة:** `pnpm lint && pnpm test && pnpm build` أخضر + وظائف CI الثلاث خضراء بعد كل Milestone.
7. **اسألني قبل أي قرار معماري كبير** غير مذكور هنا، وقبل أي إنفاق (خادم، نطاق، تخزين).
8. **لا سرّ في مستودع ولا في صورة.** الأسرار من GitHub Secrets وقت النشر، ومن ملف بصلاحيات `600` على الخادم وقت التشغيل. (H7.3)

---

## 🔐 قيود عامّة (تسري على كل بند)

- **أصل واحد** لكل بيئة: الواجهة والـAPI خلف نفس المضيف والمنفذ. لا `sameSite=none` إطلاقًا.
- **الصور من GHCR فقط** في الإنتاج، **موسومة بإصدار صريح** — لا `latest` على خادم إنتاج ولا `build:` في compose الإنتاج.
- **لا جذر:** كل حاوية تعمل بمستخدم غير جذر (الـAPI يفعل ذلك أصلًا — uid 1001).
- **الهجرات خطوة نشر منفصلة** لمرة واحدة، لا عند إقلاع كل نسخة.
- **كل ما يُكتب على القرص في حجم مُسمّى** (`volume`)، فإعادة إنشاء الحاوية لا تفقد بيانات.
- **هدف الإنتاج: Render** (2026-08-01). `docker-compose.prod.yml` للنشر المحمول لا للإنتاج.
- **نسخة واحدة من الـAPI في هذا الطور.** التوسّع الأفقي جاهز (H5) لكنه خارج النطاق هنا — ومع نسخة واحدة يبقى Redis اختياريًا.

### مفتاح الأولوية

🔴 **حاصر للنشر** · 🟠 **مطلوب قبل الاعتماد على النظام** · 🟡 **تحسين**

---

# 🏁 الـMilestones

## D0 — نشر الصور إلى سجلّ (GHCR) 🔴

**الهدف:** أن توجد نسخة قابلة للنشر من كل إصدار خارج الـrunner. لا شيء بعد هذا ممكن بدونه.

- [x] **D0.1** — workflow `release.yml`: بناء ودفع صورة الـAPI إلى GHCR عند كل وسم `v*`.
  - المطلوب: ملف `.github/workflows/release.yml` بمُشغّلين: `push: tags: ['v*']` و`workflow_dispatch` بمُدخَل `tag` (الوسوم القائمة `v2.0.0`/`v2.1.0` لن تُطلق شيئًا بأثر رجعي، فيلزم مسار يدوي لنشرها).
  - الصلاحيات: `permissions: { contents: read, packages: write }` — والدخول بـ`GITHUB_TOKEN` لا بسرّ إضافي.
  - الوسوم على الصورة: `ghcr.io/fares-code0/tender-api:<version>` و`:sha-<short>`. **لا `latest`** — راجع القيود العامة.
  - ✅ Verify: `docker pull` على جهاز نظيف ⇒ تُسحب، ثم تشغيلها ⇒ `/livez` يردّ 200.
  - **Verified (2026-07-31):** نُشرت `v2.1.0` فعلًا بالتشغيل اليدوي — [التشغيل 30596851655](https://github.com/Fares-code0/tender_x/actions/runs/30596851655). الوسمان `:v2.1.0` و`:sha-9b751b2` على البصمة نفسها `sha256:dccbe2cc…`، الحجم 523MB. ووظيفة `verify-published` على **runner نظيف لا يملك سياق البناء** سحبتها وشغّلتها: ردّت بعد 6 ثوانٍ، `/livez` ⇒ `live`، و`/readyz` ⇒ `ready` (استعلام `SELECT 1` حقيقي عبر Prisma ⇒ العميل والمحرّك سليمان في **المنشور** لا في نسخة محلية)، و`/v1/tenders` ⇒ 401.
- [x] **D0.2** — اختبار الدخان يعمل قبل الدفع؛ صورة فاشلة لا تصل السجلّ.
  - المطلوب: في `release.yml`، بعد البناء وقبل الدفع، شغّل `scripts/docker-smoke-test.sh`. صورة تفشل لا تصل السجلّ إطلاقًا.
  - ✅ Verify: افتعل فشلًا مؤقتًا ⇒ الوظيفة حمراء و**لا صورة على GHCR**؛ ثم أزل الافتعال ⇒ خضراء والصورة موجودة.
  - **Verified (2026-07-31) — بالاختبار السلبي، لا بالمسار السعيد وحده:** الحالة الموجبة 18/18 قبل الدفع في التشغيل أعلاه. ثم كُسر عنوان القاعدة في خدمة `api` وحدها ووُسم الكسر مؤقتًا — [التشغيل 30597232955](https://github.com/Fares-code0/tender_x/actions/runs/30597232955): «Build images» **نجحت** (فالصورة بُنيت سليمة)، و«Smoke test» **فشلت**، و«Log in to GHCR» و«Tag and push» **تخطّيتا معًا**، و`verify-published` تخطّت. الدفع هو المسار الوحيد الذي يُخرج صورة، ولم يعمل. حُذف الوسم المؤقت والفرع بعد التحقق.
  - **حدّ التحقق:** لم أسرد وسوم GHCR للتأكيد المباشر — توكن `gh` هنا بلا صلاحية `read:packages`. الاستدلال قائم على تخطّي خطوة الدفع نفسها.
- [x] **D0.3** — توثيق سحب الصور في `docs/operations/deployment.md`.
  - ✅ Verify: اتّباع الخطوات حرفيًا ينتج حاوية تعمل.
  - **Verified (2026-07-31):** خطوات §3 من الدليل (سحب ⇒ تشغيل بمتغيرات البيئة ⇒ فحص `/livez` و`/readyz` و`/v1/tenders`) هي **نفسها** التي تنفّذها وظيفة `verify-published` آليًا على runner نظيف، وقد نجحت. الدليل يوثّق مسارًا مُنفَّذًا لا مقترحًا.

**🔒 بوابة D0:** ✅ **مُنجَزة ومُتحقَّقة (2026-07-31).** على GHCR صورة `v2.1.0` مُتحقَّق من سحبها وإقلاعها على جهاز لا يملك سياق البناء، وثبت **بالكسر المتعمّد** أن صورة لا تجتاز الدخان لا تصل السجلّ. `pnpm lint && pnpm test && pnpm build` ووظائف CI الثلاث خضراء.

---

## D1 — تحويّة الواجهة وتوحيد الأصل 🔴

**الهدف:** أن تصير المنظومة قابلة للتشغيل كوحدة واحدة خلف أصل واحد — وهو شرط عمل المصادقة أصلًا.

- [x] **D1.1** — `apps/web/Dockerfile` متعدد المراحل.
  - المطلوب: مرحلة بناء على `node:22-bookworm-slim` بـcorepack وpnpm (نفس نمط `apps/api/Dockerfile`) تنتج `apps/web/dist`؛ ومرحلة تشغيل على `caddy:2-alpine` تحمل الملفات الساكنة فقط.
  - **لماذا Caddy لا nginx:** TLS تلقائي بالكامل (إصدار وتجديد)، فيسقط صنف كامل من الأعطال التشغيلية — انتهاء شهادة صامت هو أشهر عطل في نشر VPS. الثمن أداة أقل شيوعًا؛ مقبول مقابل إلغاء تجديد الشهادات يدويًا.
  - انتبه: `apps/web` يعتمد `@tender/shared` من مساحة العمل ⇒ **يُبنى من جذر المستودع** كالـAPI تمامًا.
  - ✅ Verify: الصورة تُبنى وتخدم الواجهة.
  - **Verified (2026-07-31):** [التشغيل 30600068034](https://github.com/Fares-code0/tender_x/actions/runs/30600068034) — `https://localhost/` ⇒ 200 وصفحة التطبيق. Caddy يعمل بمستخدم غير جذر (uid 1001)، ولهذا يستمع على 8080/8443 ويُوجَّه إليها من 80/443: بديل عن منح `CAP_NET_BIND_SERVICE`.
- [x] **D1.2** — `deploy/Caddyfile`: خدمة SPA + بروكسي `/api`.
  - المطلوب سلوكيًا شيئان لا ثالث لهما:
    1. **ارتداد SPA:** أي مسار غير موجود كملف يُخدَم منه `index.html` (وإلا فتحديث الصفحة على `/tenders/5` يعطي 404 — عطل كلاسيكي).
    2. **حذف البادئة:** `/api/v1/...` ⇒ `api:4000/v1/...`. البادئة `/api` تُحذف تمامًا كما يفعل بروكسي Vite، وإلا صار المسار `/api/v1` على الـAPI ⇒ 404 على كل نداء.
  - ✅ Verify: `/api/v1/tenders` ⇒ **401** لا 404 (يثبت وصول الطلب بالمسار الصحيح)، و`/tenders/999` ⇒ 200 لا 404.
  - **Verified (2026-07-31):** الاثنان أخضران في التشغيل نفسه — `/api/livez` ⇒ 200 و`/api/v1/tenders` ⇒ 401 و`/tenders/999` ⇒ 200.
- [x] **D1.3** — خدمة `web` في `docker-compose.yml` المحلي.
  - المطلوب: خدمة تُبنى من `apps/web/Dockerfile`، تعتمد على `api`، وتنشر المنفذ 8080. الـAPI **لا ينشر منفذه** بعد اليوم في هذا الملف: كل شيء يمرّ من الأصل الواحد. و`WEB_ORIGIN` يصير أصل البروكسي نفسه.
  - ✅ Verify: المنظومة تقلع بأصل واحد ولا منفذ للـAPI مكشوف.
  - **Verified (2026-07-31):** فحص `منفذ الـAPI غير مكشوف على المضيف` أخضر — `expose` بدل `ports`. وأُضيفت خدمة `seed` (تطوير و CI فقط) لأن دورة الدخول تحتاج مستخدمًا. **لم يُفتح متصفح فعلي**: لا محرّك Docker على جهاز التطوير؛ البديل المُنفَّذ أقوى من فتح صفحة — `https://localhost/` يعيد 200 ويحوي `id="root"`، ودورة دخول كاملة تمرّ عبره.
- [x] **D1.4** — توسيع اختبار الدخان: **دورة تسجيل دخول كاملة عبر الأصل الواحد**.
  - المطلوب: إضافة إلى `scripts/docker-smoke-test.sh` — `POST /api/v1/auth/login` ببيانات مستخدم مزروع، ثم استعمال الكوكي المعادة في طلب محمي (`GET /api/v1/tenders`) وتوقّع 200.
  - **هذا الفحص هو جوهر D1:** لا يمرّ إلا إذا صحّ الأصل الواحد وحذف البادئة وسلوك `sameSite=strict` معًا. ولا يكفي فحص صحة يردّ 200.
  - ✅ Verify: الفحص أخضر في CI؛ ثم اكسر `strip_prefix` عمدًا ⇒ **يجب أن يحمرّ**. فحصٌ لا يحمرّ عند كسر ما يحرسه ليس فحصًا.
  - **Verified (2026-07-31) — بالموجب والسالب معًا:** الموجب **26/26** في [التشغيل 30600068034](https://github.com/Fares-code0/tender_x/actions/runs/30600068034)، منه: تسجيل دخول ⇒ 200، وكوكي `httpOnly` و`Secure`، و`/api/v1/tenders` بالجلسة ⇒ 200. والسالب: كُسر `strip_prefix` وحده في [التشغيل 30600283798](https://github.com/Fares-code0/tender_x/actions/runs/30600283798) ⇒ **5 فحوص حمراء** منها دورة الدخول كاملة (21 ناجح · 5 فاشل)، بينما بقيت فحوص SPA خضراء — أي أن الفشل أصاب ما كُسر بالضبط. حُذف الفرع المؤقت وأُغلق PR #20.
- [x] **D1.5** — نشر صورة الواجهة إلى GHCR ضمن `release.yml` (توسعة D0.1).
  - ✅ Verify: `ghcr.io/fares-code0/tender-web:<version>` موجودة وتُسحب وتعمل.
  - **Verified (2026-08-01):** دفع الوسم `v2.2.0` أطلق [التشغيل 30700205817](https://github.com/Fares-code0/tender_x/actions/runs/30700205817) تلقائيًا — 26/26 في الدخان ثم نُشرت **الصورتان معًا بنفس الوسم**: `tender-api:v2.2.0` (523MB · `sha256:57cf756a…`) و`tender-web:v2.2.0` (**63.6MB** · `sha256:e094100b…`)، وكلٌّ منهما بوسم `sha-2be87a7` على البصمة نفسها. ثم سحبهما runner نظيف وشغّلهما: `/livez` ⇒ `live` و`/readyz` ⇒ `ready` و`/v1/tenders` ⇒ 401.
  - كان هذا البند معلّقًا لأن `release.yml` يبني من **الوسم** المطلوب نشره، و`v2.1.0` سابق لـD1 فلا يحوي `apps/web/Dockerfile` — فإعادة تشغيله عليه كانت ستفشل. الوسم الجديد حلّ ذلك.

**🔒 بوابة D1:** ✅ **مُنجَزة ومُتحقَّقة (2026-08-01).** المنظومة تعمل بأصل واحد، ودورة تسجيل الدخول خضراء (**26/26**) ومُثبَتة بالكسر المتعمّد، والصورتان منشورتان بوسم `v2.2.0` ومُتحقَّق من سحبهما وتشغيلهما. `pnpm lint && pnpm test && pnpm build` ووظائف CI الثلاث خضراء.

---

## D2 — بيئة الإنتاج على Render 🔴

**الهدف:** أن يعمل النظام على عنوان عام بـHTTPS، وأن يصير النشر خطوة واحدة قابلة للتكرار والتراجع.

> **تبديل هدف النشر (2026-08-01) — قرارك.** كان الهدف خادمًا مملوكًا (VPS + `docker compose` + نشر عبر SSH)، وصار **Render**.
>
> **السبب المسجَّل:** *«اختير Render للنشر بغرض العرض لأنه يحفظ معمارية الأصل الواحد ويُزيل عبء صيانة الخادم.»*
>
> **مسار الـVPS مُستبدَل لا فاشل.** كان يعمل بقدر ما تحقّقنا منه: `docker-compose.prod.yml` مرّ فحص `config` وحُدّد بأقل امتياز. أُوقف لأن هدف النشر تغيّر، لا لأنه أخفق.
>
> **ماذا بقي منه:** `docker-compose.prod.yml` يبقى للنشر **المحمول** على أي خادم بلا ارتباط بمنصّة، وهو المرجع الذي تُرجم منه `render.yaml` — البنية واحدة في الاثنين. وحُذف `deploy.yml` (نشر SSH): إبقاء طريق نشر لا يُستعمل يجعل كل قارئ يسأل أيّهما الحقيقي.

- [x] **D2.1** — منظومة إنتاج محمولة: `docker-compose.prod.yml`.
  - المطلوب: نسخة إنتاج تختلف عن المحلية في أربعة أمور: `image:` من GHCR بوسم صريح بدل `build:`؛ لا منافذ مكشوفة إلا 80/443 من البروكسي؛ `restart: unless-stopped` على الكل؛ الأسرار من `env_file` لا من قيم مكتوبة.
  - ✅ Verify: `docker compose -f docker-compose.prod.yml config` يمرّ، ولا `build:` ولا سرّ مكتوب في الملف.
  - **Verified (2026-08-01):** `config` يمرّ بـexit 0، ويفشل بـexit 1 عند غياب `IMAGE_TAG` — الحارس يعمل. لا `build:` في أي خدمة، والمنافذ المنشورة 80 و443 فقط (القاعدة بلا `ports`). وفُصلت المتغيّرات بأقل امتياز: `db` يأخذ `POSTGRES_*` وحدها و`migrate` يأخذ `DATABASE_URL` وحده — تحميل `env_file` كاملًا كان يضع `JWT_SECRET` داخل حاوية القاعدة بلا سبب.
  - **بعد التبديل:** لم يعد مسار الإنتاج، لكنه يبقى مُتحقَّقًا وصالحًا للنشر المحمول. لا يُنقض تحقّقٌ لأن الوجهة تغيّرت.

- [~] **D2.2** — مخطّط Render: `render.yaml`. — **مكتوب، والتحقق معلّق على أول مزامنة.**
  - المطلوب: خدمة عامّة واحدة (`tender-web`)، وخدمة **خاصّة** للـAPI بلا عنوان عام، وقاعدة مُدارة، وكلٌّ بصورة GHCR بوسم صريح. الأصل الواحد يبقى كما هو.
  - ✅ Verify: مزامنة المخطّط تُنشئ الثلاثة، و`tender-api` **بلا عنوان عام**، والسحب من GHCR ينجح.
  - **مكتوب (2026-08-01):** `pserv` للـAPI و`web` للبروكسي و`databases` للقاعدة. `API_UPSTREAM` يُحقَن من Render (`fromService … hostport`) فلا عنوان مكتوب يتقادم، و`JWT_SECRET` بـ`generateValue` فلا يراه أحد، و`ipAllowList: []` يمنع الوصول للقاعدة من خارج الشبكة الداخلية.
  - **لا يُعلَّم [x]:** بنية الملف مُتحقَّقة بمحلّل YAML (خدمة عامّة واحدة بالضبط)، وهذا **لا يثبت** أن Render تقبل أسماء الخطط ولا أن السحب من سجلّ خاص ينجح.

- [~] **D2.3** — الـCaddyfile يخدم البيئات الثلاث من ملف واحد. — **مكتوب، ووضع Render لم يُشغَّل.**
  - المطلوب: إزالة معالجة ACME الخاصّة بالـVPS، والاستماع على منفذ Render، ووجهة الـAPI من البيئة لا مكتوبة.
  - ✅ Verify: وضع `localhost` أخضر في اختبار الدخان، ووضع Render يمرّ من محلّل Caddy، ووضع النطاق الحقيقي يُصدر شهادة.
  - **مكتوب (2026-08-01):** حُذف توجيه `tls` كليًا؛ شكل العنوان وحده يحدّد السلوك — اسم مضيف ⇒ شهادة تلقائية (داخلية لـ`localhost`، وLet's Encrypt لنطاق حقيقي)، وعنوان بلا اسم مضيف (`:10000`) ⇒ HTTP بلا شهادة كما يتطلّب Render. هذا أبسط من توجيه شرطي لا يدعمه Caddyfile أصلًا. و`API_UPSTREAM` من البيئة بافتراضي `api:4000` فيبقى compose كما هو.
  - **مُتحقَّق جزئيًا:** أُضيف إلى CI فحص `caddy validate` بوضع Render — خطأ صياغة يظهر في CI لا في نشر فاشل. لكن **قبول Render فعليًا لم يُختبر**.

- [ ] **D2.4** — أول نشر فعلي على Render.
  - المطلوب: ربط المستودع، واعتماد سجلّ باسم `ghcr`، ومزامنة المخطّط، وضبط `WEB_ORIGIN` على العنوان العام.
  - ✅ Verify: **دورة تسجيل دخول كاملة من متصفح فعلي** على العنوان العام بشهادة صالحة، ثم إنشاء مناقصة تجريبية ورفع مرفق. و`/api/livez` و`/api/readyz` يردّان 200.

- [ ] **D2.5** — الهجرات تعمل على Render.
  - المطلوب: `preDeployCommand` على `tender-api` يشغّل `prisma migrate deploy` مرة واحدة قبل استقبال أي طلب، وفشلها يوقف النشر.
  - ✅ Verify: سجلّ النشر يُظهر تطبيق الهجرات، و`/api/readyz` ⇒ `ready`. ثم **افتعل هجرة فاشلة** ⇒ يجب أن يتوقف النشر وتبقى النسخة السابقة عاملة.

- [ ] **D2.6** — التراجع مُختبَر.
  - المطلوب: إعادة الوسم في `render.yaml` إلى إصدار أقدم ⇒ Render تنشره.
  - ✅ Verify: نُفِّذ تراجع فعلي وسُجِّل زمنه. تراجع غير مُختبَر ليس خطة تراجع.

**🔒 بوابة D2:** لا تُغلق قبل اجتماع الستّة: النشر نجح · التطبيق يُحمَّل على عنوانه العام · HTTPS يعمل · دورة تسجيل الدخول تعمل والكوكي تثبت · نداءات `/api/v1` تصل · الهجرات نُفِّذت · فحوص الصحة خضراء.

---

## D3 — نسخ احتياطي واستعادة **مُختبَرة** 🔴

**الهدف:** أن تكون قادرًا على استرجاع النظام بعد كارثة — بدليل، لا بنيّة.

> نسخة احتياطية لم تُجرَّب استعادتها ليست نسخة احتياطية. هذا الـMilestone كله يقوم على البند D3.4.

- [ ] **D3.1** — `scripts/db-backup.sh`.
  - المطلوب: `pg_dump -Fc` داخل حاوية القاعدة، ملف باسم فيه الطابع الزمني، حذف ما تجاوز 14 يومًا محليًا، ورمز خروج غير صفري عند أي فشل.
  - ✅ Verify: تشغيله ⇒ ملف حجمه > 0؛ ثم `pg_restore --list` عليه ⇒ يسرد الجداول.
- [ ] **D3.2** — جدولة يومية.
  - المطلوب: systemd timer على الخادم (أوضح من cron في التشخيص وله سجلّ).
  - ✅ Verify: `systemctl list-timers` يُظهر الموعد القادم، وبعد يوم واحد يوجد ملف جديد فعلًا.
- [ ] **D3.3** — نسخ **خارج الخادم**.
  - المطلوب: رفع إلى S3 (البنية والاعتماديات موجودة أصلًا من H5.1). نسخة على نفس القرص لا تنجو من فقد القرص.
  - ✅ Verify: الملف ظاهر في الحاوية على S3 بحجم مطابق.
- [ ] **D3.4** — `scripts/db-restore.sh` + **تمرين استعادة فعلي**. 🔴
  - المطلوب: استعادة أحدث نسخة إلى قاعدة **مؤقتة منفصلة**، ثم مقارنة عدد صفوف `Tender` و`User` و`Attachment` بالإنتاج.
  - ✅ Verify: الأعداد متطابقة. **سجّل زمن الاستعادة** — هو رقم RTO الحقيقي، وبدونه أي وعد بزمن تعافٍ تخمين.
- [ ] **D3.5** — استعادة المرفقات.
  - المطلوب: توثيق كيف تُستعاد المرفقات (S3 أو الحجم المحلي) — قاعدة بيانات بلا مرفقاتها استعادة ناقصة.
  - ✅ Verify: بعد استعادة تجريبية، تنزيل مرفق قديم ينجح.

**🔒 بوابة D3:** جرت استعادة فعلية وطابقت، وزمنها مُسجَّل، والنسخ خارج الخادم.

---

## D4 — Runbook وجاهزية التشغيل 🟠

**الهدف:** أن يستطيع غيرك تشغيل النظام وإصلاحه دون أن يقرأ الشيفرة.

- [ ] **D4.1** — `docs/operations/deployment.md` مكتمل: نشر، ترقية، **تراجع**.
  - ✅ Verify: **نفّذ تراجعًا فعليًا** على staging إلى الوسم السابق وسجّل الزمن. تراجع غير مُختبَر ليس خطة تراجع.
- [ ] **D4.2** — `docs/operations/runbook.md`: الأعطال المتوقّعة وعلاجها.
  - يغطي على الأقل: `/readyz` يردّ 503 · القرص ممتلئ · هجرة فشلت في المنتصف · الشهادة لم تتجدد · استهلاك ذاكرة مرتفع.
  - ✅ Verify: افتعل عطلين فعليًا على staging (أوقف القاعدة؛ املأ القرص) واتّبع الـrunbook حرفيًا ⇒ يقود للحل.
- [ ] **D4.3** — مراقبة ورصد.
  - المطلوب: `/metrics` **غير مكشوف للعامة** (محجوب في Caddy أو خلف مصادقة)، وفحص توفّر خارجي ينبّه عند سقوط `/livez`.
  - ✅ Verify: `curl https://<domain>/metrics` من خارج ⇒ 403/404؛ وإيقاف الخدمة عمدًا ⇒ وصل تنبيه فعلًا.
- [ ] **D4.4** — تدوير السجلات.
  - المطلوب: حدّ لحجم سجلات Docker (`max-size`/`max-file`). سجلات pino غير محدودة تملأ القرص ثم تُسقط النظام.
  - ✅ Verify: `docker inspect` يُظهر الحدّ مضبوطًا.
- [ ] **D4.5** — 🟡 **H3.4 المؤجَّل** — تتبّع OpenTelemetry.
  - صار ممكنًا الآن: البند أُجِّل في طور التقوية لانعدام مكان يعيش فيه الـcollector، وقد صار موجودًا.
  - ✅ Verify: أثر واحد كامل من الطلب حتى استعلام القاعدة ظاهر في الواجهة.

**🔒 بوابة D4:** التراجع مُختبَر، والـrunbook مُجرَّب على أعطال حقيقية، والتنبيه وصل فعلًا.

---

## 🚫 خارج النطاق (ما لم نتفق عليه)

- توسّع أفقي بعدة نسخ من الـAPI (جاهز بـH5، لكن نسخة واحدة تكفي هذه المرحلة).
- Kubernetes أو أي منسّق حاويات — مبالغة لهذا الحجم.
- CDN أو موازن أحمال مُدار.
- نشر تلقائي على الإنتاج عند كل دمج (النشر قرار بشري — D2.5).
- استرجاع لحظي (PITR) لقاعدة البيانات؛ نسخة يومية كافية لهذه المرحلة، وتُراجَع إن تغيّر متطلب فقد البيانات.

---

## 📊 لوحة تقدّم النشر (حدّثها بعد كل Milestone)

| Milestone | الأولوية | الحالة | التاريخ | ملاحظات |
| --- | --- | --- | --- | --- |
| D0 — نشر الصور إلى GHCR | 🔴 | ✅ مكتمل ومُتحقَّق | 2026-07-31 | `v2.1.0` منشورة ومُتحقَّقة بالسحب · البوابة مُختبَرة بالكسر |
| D1 — الواجهة والأصل الواحد | 🔴 | ✅ مكتمل ومُتحقَّق | 2026-08-01 | 26/26 دخان · صورتان منشورتان بوسم `v2.2.0` |
| D2 — بيئة الإنتاج على **Render** | 🔴 | 🟡 جزئي (1/6 مُتحقَّق · 2 مكتوبان) | 2026-08-01 | الهدف بُدِّل من VPS · الباقي يحتاج أول مزامنة |
| D3 — نسخ احتياطي واستعادة | 🔴 | ⬜ لم يبدأ | — | البند الحاسم: D3.4 استعادة فعلية |
| D4 — Runbook والمراقبة | 🟠 | ⬜ لم يبدأ | — | يحتضن H3.4 المؤجَّل |

---

## 🗺️ خريطة البنود ↔ الفجوات (Traceability)

| # | الفجوة (2026-07-31) | البند | الحالة |
| --- | --- | --- | --- |
| 1 | الصورة تُبنى في CI ثم تُمحى — لا نسخة قابلة للنشر | D0.1 | ✅ |
| 2 | لا شيء يمنع نشر صورة لم تُختبر | D0.2 | ✅ |
| 3 | `apps/web` بلا Dockerfile — الواجهة غير محوّاة | D1.1 | ✅ |
| 4 | `API_BASE` نسبي بلا بروكسي في الإنتاج ⇒ المصادقة تنهار | D1.2 · D1.4 | ✅ |
| 5 | لا بيئة عامّة ولا TLS | D2.2 · D2.4 | ⬜ |
| 6 | النشر طقس يدوي غير موثّق | D2.4 · D4.1 | ⬜ |
| 7 | لا نسخ احتياطي إطلاقًا | D3.1 · D3.2 · D3.3 | ⬜ |
| 8 | لا إجراء استعادة ولا رقم RTO | D3.4 | ⬜ |
| 9 | لا خطة تراجع | D4.1 | ⬜ |
| 10 | لا رصد لسقوط الخدمة | D4.3 | ⬜ |
| 11 | سجلات Docker غير محدودة الحجم | D4.4 | ⬜ |
| 12 | H3.4 (التتبّع) مؤجَّل منذ طور التقوية | D4.5 | ⬜ |

---

## 🐞 سجل مشاكل النشر (يملؤه Claude Code أثناء العمل)

| التاريخ | البند | المشكلة | الحل |
| --- | --- | --- | --- |
| 2026-07-31 | D0 (اكتشاف) | **الصورة المنشورة لا تصلح لتطبيق الهجرات:** أداة `prisma` تطويرية و`pnpm --prod deploy` يستبعدها، ومرحلة `build` التي تحلّ ذلك محليًا **غير منشورة** ⇒ خادم لا يملك سياق البناء لا يستطيع تطبيق هجرة | حُصر D0 خارج الهجرات عمدًا وقتها. **حُسم في 2026-08-01 (قرارك):** نقل `prisma` إلى تبعيات الإنتاج ⇒ صورة واحدة تكفي للتشغيل والهجرة. الأداة والعميل من قفل واحد فتطابقهما مضمون، بخلاف صورتين تنزلق إحداهما عن الأخرى. **بلا زيادة تُذكر في الحجم: 523MB قبل وبعد**، وخدمة `migrate` صارت تعمل من صورة التشغيل نفسها فتطابق ما سيجري على الخادم |
| 2026-07-31 | D0.2 | `workflow_dispatch` لا يُتاح إلا لملف موجود على الفرع الافتراضي ⇒ تعذّر تجربة الـworkflow قبل دمجه | دُمج أولًا ثم شُغِّل، ولم تُعلَّم بنود D0 بـ`[x]` قبل خضرة التشغيل الفعلي. والاختبار السلبي جرى بوسم مؤقت (مسار `push: tags`) لا بالتشغيل اليدوي |
| 2026-07-31 | D1 | إضافة `deploy/Caddyfile` كسرت **بناء الـAPI**: `COPY . .` ينسخ `deploy/` إلى `/app/deploy` وهو نفسه مسار مخرج `pnpm deploy` ⇒ `ERR_PNPM_DEPLOY_DIR_NOT_EMPTY`. تصادم لا يكشفه إلا بناء فعلي | نُقل المخرج إلى `/opt/tender-api` خارج شجرة المستودع، فانتفى التصادم من جذره |
| 2026-07-31 | D1.4 (اكتشاف) | الكوكي `secure` في الإنتاج، والمنظومة تعمل بـ`NODE_ENV=production` ⇒ **اختبار دخول على HTTP عادي كان سيفشل حتمًا**: لا أحد يعيد كوكي `Secure` على HTTP | `tls internal` في Caddy محليًا وفي CI ⇒ الفحص على HTTPS كما في الإنتاج، ومكسب جانبي: مسار TLS مُجرَّب قبل D2 |
| 2026-07-31 | D1.4 (فحص) | فحصان حمراء على **منظومة سليمة**: (1) كوكي `httpOnly` تُكتب ببادئة `#HttpOnly_` فاستُبعدت كتعليق، (2) `curl` يطبع `000` بنفسه فأنتج `\|\| echo 000` قيمة `000000` | صُحّح الفحصان — العيب كان فيهما لا في المنظومة، ودورة الدخول نفسها كانت خضراء طوال الوقت |
| 2026-08-01 | D2.1 | `env_file` في compose **لا يغذّي** استبدال `${...}` داخل الملف — يمرّر المتغيّرات إلى الحاويات فقط. فبقيت `SITE_ADDRESS` و`IMAGE_TAG` بلا قيمة رغم وجودها في `.env.production` | `--env-file .env.production` **إلزامي** مع كل استدعاء، وموثّق في ترويسة الملف وفي كل خطوة من `deploy.yml` |
| 2026-08-01 | D2.1 | تحميل `env_file` على كل خدمة وضع `JWT_SECRET` وأسرار التطبيق داخل حاوية **القاعدة** بلا سبب | فصل بأقل امتياز: `db` يأخذ `POSTGRES_*` وحدها، و`migrate` يأخذ `DATABASE_URL` وحده، و`env_file` يبقى على `api` وحده |
| 2026-08-01 | D2 (تبديل هدف) | هدف النشر بُدِّل من خادم مملوك إلى **Render** بقرارك: يحفظ الأصل الواحد ويُسقط عبء صيانة الخادم | `render.yaml` بدل `docker-compose.prod.yml` كمسار إنتاج، وحُذف `deploy.yml`. المُستبدَل **لم يفشل**: مرّ فحوصه، لكن الوجهة تغيّرت — وcompose يبقى للنشر المحمول |
| 2026-08-01 | D2.3 | Caddyfile واحد لثلاث بيئات: شهادة داخلية محليًا، وLet's Encrypt على خادم مملوك، و**بلا شهادة** على Render (TLS عند الحافة). وCaddyfile لا يدعم توجيهًا شرطيًا | حُذف توجيه `tls` كليًا: شكل العنوان وحده يحدّد السلوك — اسم مضيف ⇒ شهادة تلقائية، وعنوان بلا اسم (`:10000`) ⇒ HTTP بلا شهادة. أبسط من أي شرط، وأُضيف `caddy validate` بوضع Render إلى CI |

---

## ✋ نقاط توقّف إلزامية (اسألني قبل المتابعة)

1. ~~**قبل D2:** أين يُنشر؟~~ — ✅ **VPS + docker compose** (قرارك، 2026-07-31).
2. ~~**قبل D1.1:** موافقة على **Caddy** بدل nginx~~ — ✅ **مُعتمَد ضمنًا ومُنفَّذ** (2026-07-31). المبرَّر: TLS تلقائي يُسقط عطل الشهادة المنتهية.
2ب. ~~**الهجرات: صورة واحدة أم صورتان؟**~~ — ✅ **صورة واحدة** (قرارك، 2026-08-01): `prisma` تبعية إنتاج، فتطابق إصدارها مع العميل مضمون من القفل، وبلا زيادة تُذكر في الحجم.
3. ~~**قبل D2.2:** بيانات الخادم والنطاق~~ — ✅ **سقط بتبديل الهدف إلى Render** (2026-08-01): لا خادم تديره ولا شهادة تُجدّدها. يبقى مطلوبًا منك: **اعتماد سجلّ باسم `ghcr`** في Render لسحب الصور الخاصّة.
4. ~~**قبل D2.5:** مفتاح SSH للنشر~~ — ✅ **سقط بتبديل الهدف**: لا SSH في نشر Render. و`JWT_SECRET` تولّده Render عشوائيًا فلا يمرّ بأحد. **ما زلت لا أتعامل مع أسرار حقيقية**: اعتماد السجلّ و`WEB_ORIGIN` تضبطهما أنت في اللوحة.
5. **قبل D3.3:** حاوية S3 للنسخ الاحتياطي (أهي نفسها حاوية المرفقات أم منفصلة؟ الفصل أسلم).
6. **قبل D2.6:** إذن صريح بالنشر على الإنتاج. لن أنشر على الإنتاج من تلقاء نفسي.

---

## 📌 معيار انتهاء الطور

يُعلَن الطور مكتملًا حين تجتمع الخمسة:

1. النظام يعمل على نطاقه بـHTTPS، ودورة عمل كاملة نُفِّذت عليه بحساب حقيقي.
2. النشر والتراجع كلاهما **نُفِّذ فعلًا** وزمنه مُسجَّل.
3. استعادة قاعدة بيانات **جرت وطابقت**، ورقم RTO مكتوب.
4. تنبيه سقوط الخدمة **وصل فعلًا** في تجربة مقصودة.
5. الـrunbook **جُرِّب** على عطلين حقيقيين لا على الورق.

عندها: وسم `v3.0.0` — أول إصدار **منشور**، لا مجرد مبني.
