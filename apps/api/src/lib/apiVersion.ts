/**
 * H6.3 — بادئة نسخة الـAPI.
 *
 * معرَّفة مرة واحدة ليستعملها التطبيق والاختبارات معًا، فلا تتناثر السلسلة
 * `'/v1'` في عشرات المواضع عند تقديم `/v2`.
 */
export const API_V1 = '/v1';

/** فحوص البنية التي تبقى **بلا** نسخة (عقد مع المنسّق/الكاشط لا مع عملاء الـAPI) */
export const UNVERSIONED_PATHS = ['/health', '/livez', '/readyz', '/metrics'] as const;
