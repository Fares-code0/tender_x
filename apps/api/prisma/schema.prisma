generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// جدول إعدادات النظام (BR-009) — الكيانات الكاملة تُضاف في M1.1
model SystemSetting {
  key       String   @id
  value     String
  updatedAt DateTime @updatedAt
}
